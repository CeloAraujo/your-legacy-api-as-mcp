{
  "name": "testes-com-bancos-de-dados",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "cross-env DB_NAME=customers node src/index.js",
    "dev": "cross-env DB_NAME=customers node --watch src/index.js",
    "test": "cross-env NODE_ENV=test node --experimental-test-coverage --test test/*.test.js",
    "test:dev": "cross-env NODE_ENV=test node --watch --test test/*.test.js",
    "test:debug": "cross-env NODE_ENV=test node --inspect --test --watch test/*.test.js",
    "docker:infra:up": "docker compose up -d --wait",
    "docker:infra:down": "docker compose down",
    "docker:infra:cleanup": "docker compose down --volumes --remove-orphans && rm -rf storage",
    "docker:infra:logs": "docker compose logs -f"
  },
  "keywords": [],
  "author": "marceloaraujo",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20.11.0"
  },
  "dependencies": {
    "fastify": "^4.26.1",
    "mongodb": "^6.3.0"
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}
